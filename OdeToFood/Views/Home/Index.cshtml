@model OdeToFood.ViewModels.HomeIndexViewModel

@{
    ViewBag.Title = "Homey page";
}


<h1>All Restaurants</h1>
@foreach (var res in Model.Restaurants)
{
    @Html.Partial("_Summary", res)
}

@*<h1>old way Restaurants</h1>
<table>
    @foreach (var res in Model.Restaurants)
    {
        <tr>
            <td>@res.Id </td>
            <td>@res.Name</td>
            <td>
                <a asp-action="Details" asp-route-id="@res.Id">More</a>
                <a asp-page="/Restaurants/Edit" asp-route-id="@res.Id">Edit</a>
            </td>
        </tr>
    }
</table>*@
<a class="btn btn-primary" asp-action="Create">Add Restaurant</a>

@* commented out*@
@*@section footer {
    @Model.CurrentMessage
}*@

